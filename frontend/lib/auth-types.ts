// Auth + business-settings shapes from the gateway
// Re-exported from the main auth/types modules for ergonomic imports
// inside the dashboard.

export type { AuthUser } from './auth'
export type { BusinessSettings } from './types'
